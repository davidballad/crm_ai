# =============================================================================
# Secrets (never commit). Copy to secrets.tfvars and fill in.
#   cp secrets.tfvars.example secrets.tfvars
# =============================================================================

gemini_api_key  = "YOUR_GEMINI_API_KEY"
service_api_key = "YOUR_SERVICE_API_KEY"
